# AWS / Bedrock
AWS_REGION=us-east-1
# ou use BEDROCK_AWS_REGION se preferir
# BEDROCK_AWS_REGION=us-east-1
# Se usar credenciais por variáveis de ambiente
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...

# ID do modelo no Bedrock (confira o disponível na sua região)
BEDROCK_MODEL_ID=anthropic.claude-3-5-sonnet-20240620-v1:0
BEDROCK_TEMPERATURE=0

# MCP
# Muitos servidores MCP HTTP expõem em /mcp (com ou sem barra final)
MCP_URL=http://localhost:4002/mcp
# Se seu servidor não usa /mcp, mude para http://localhost:4002
# Token opcional, se o MCP exigir auth
# MCP_AUTH_TOKEN=seu_token